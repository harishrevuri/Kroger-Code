options:
  cors: true
tests:
  test:
    action: dropx/products.get
    input: {}
  test1:
    action: dropx/products.get
    input:
      pids: rays
    accounts: {}
paths:
  /paths0:
    get:
      action: dropx/products.get
      accounts: {}
